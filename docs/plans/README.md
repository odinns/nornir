# Legacy plans

`docs/plans/` is historical planning material.

Do not use these files as the active roadmap or backlog for Nornir.

Active work now lives in `/Users/odinn/Projects/runes/nornir/` as atomic Runes items:

- `bugs/`
- `features/`
- `tasks/`
- `ideas/`

Keep the old phase docs for context, architecture history, and references that still help explain why something exists. If a plan needs to become current work, capture it in Runes instead of extending the legacy phase files.
